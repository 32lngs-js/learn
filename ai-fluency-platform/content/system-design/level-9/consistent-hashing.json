{"meta":{"title":"9.6 Consistent Hashing","description":"How consistent hashing distributes load, virtual nodes, and why it matters for caches and databases.","level":"level-9","slug":"consistent-hashing","order":6,"isCheckpoint":false,"isIndex":false},"blocks":[{"type":"predictPrompt","prompt":"You have 10 cache servers. Using hash(key) % 10 to assign keys works fine — until you add an 11th server. Now hash(key) % 11 reassigns almost every key. Is there a better approach?"},{"type":"markdown","content":"## The problem with modulo hashing\n\n`server = hash(key) % N` works but adding or removing a server changes N, causing nearly all keys to be reassigned. For a cache, this means a mass cache miss (cache stampede).\n\n## Consistent hashing\n\nArrange the hash space as a ring (0 to 2^32). Place servers at positions on the ring (hash their address). To find a key's server, hash the key and walk clockwise to the first server.\n\n```\n       Server A\n      /         \\\n   Key1          Server B\n    |              |\n   Key2          Key3\n      \\         /\n       Server C\n```\n\n**Adding a server:** Only keys between the new server and its predecessor are reassigned. All other keys stay put.\n\n**Removing a server:** Only that server's keys move to the next server clockwise.\n\n### Virtual nodes\n\nWith few physical servers, distribution can be uneven. Solution: each physical server gets multiple virtual nodes (100-200) spread around the ring. This ensures even distribution.\n\n**Used by:** DynamoDB (partitioning), Cassandra, Memcached (client-side), Akamai CDN.\n\n### Impact\n\nWith N servers, adding/removing a server only reassigns ~1/N of keys (instead of nearly all with modulo hashing). For a 100-server cache cluster, that is 1% of keys instead of 99%."},{"type":"keyTakeaway","content":"Consistent hashing minimizes key redistribution when servers change — only ~1/N of keys move. Virtual nodes ensure even distribution. Used by virtually every distributed cache, database, and CDN for load distribution."},{"type":"connectPrompt","prompt":"Module 9.7 covers gossip protocols — how nodes discover each other and share state without a central coordinator."}]}
