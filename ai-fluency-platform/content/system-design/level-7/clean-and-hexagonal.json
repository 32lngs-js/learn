{"meta":{"title":"7.4 Clean and Hexagonal Architecture","description":"Ports and adapters, dependency inversion at the system level, and testing boundaries.","level":"level-7","slug":"clean-and-hexagonal","order":4,"isCheckpoint":false,"isIndex":false},"blocks":[{"type":"predictPrompt","prompt":"Your service uses PostgreSQL today. Next quarter, you need to switch to DynamoDB. How much of your code needs to change? If the answer is 'most of it,' what went wrong in your architecture?"},{"type":"markdown","content":"## Hexagonal Architecture (Ports and Adapters)\n\nThe core business logic is at the center, surrounded by ports (interfaces) and adapters (implementations).\n\n```\n     ┌──────────────────────┐\n     │      Adapters         │\n     │  (REST API, CLI, UI)  │\n     │         ↓ Port         │\n     │  ┌──────────────┐     │\n     │  │  Core Domain  │     │\n     │  │  (Business    │     │\n     │  │   Logic)      │     │\n     │  └──────────────┘     │\n     │         ↓ Port         │\n     │  (DB, Queue, API)     │\n     └──────────────────────┘\n```\n\n**Port:** An interface that the core defines. \"I need to save an order\" (not \"I need PostgreSQL\").\n**Adapter:** An implementation of a port. PostgresOrderRepository implements OrderRepository.\n\n### Dependency Rule\n\nDependencies point inward. The core knows nothing about adapters. Adapters depend on the core.\n\n- Core defines: `interface OrderRepository { save(order): void }`\n- Adapter implements: `class PostgresOrderRepository implements OrderRepository`\n- Switching to DynamoDB: write `class DynamoOrderRepository implements OrderRepository`. Core unchanged.\n\n### Testing boundaries\n\nWith ports and adapters, testing is natural:\n- **Unit tests:** Test core logic with mock adapters (in-memory repositories)\n- **Integration tests:** Test adapters against real infrastructure (test database)\n- **End-to-end tests:** Test through the API adapter\n\n### When layered architecture is fine\n\nFor simple CRUD services, hexagonal architecture adds unnecessary abstraction. A three-layer architecture (controller → service → repository) is sufficient when the service is simple and unlikely to change its infrastructure."},{"type":"keyTakeaway","content":"Hexagonal architecture isolates business logic from infrastructure through ports (interfaces) and adapters (implementations). Dependencies point inward. This enables easy testing with mocks and painless infrastructure changes. Use it for complex services; simple layered architecture is fine for CRUD."},{"type":"connectPrompt","prompt":"Module 7.5 covers deployment patterns — how to release changes safely with strangler fig, blue-green, and canary deployments."}]}
